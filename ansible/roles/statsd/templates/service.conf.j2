description "Statsd"
start on runlevel [2345]
stop on runlevel [06]
# set max file descriptors to 65536 (soft/hard)
limit nofile 65536 65536

respawn

script
        chdir {{ statsd_config_path }}
        exec statsd statsd.conf >> "{{ statsd_log_directory }}statsd.log"
end script